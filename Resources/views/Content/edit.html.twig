{% extends templatePath('Widgets/Template:edit.html.twig', 'admin') %}
{% block header_text %}Edit Content{% endblock %}
{% block subheader_text %}{{ entity.name }}{% endblock %}
{% block above_form %}
    <div class="row">
        <div class="col-xs-12 header-buttons">
            <a href="{{ renderGridBackUrl('cart_admin_content') }}" class="btn btn-default">Back</a>
            <a href="{{ path('cart_content_view', {'slug': entity.slug}) }}" target="_blank" class="btn btn-default">Preview</a>
        </div>
    </div>
{% endblock %}
